package student_classes;
/**
 * <code>public enum</code> (enumeration) that provides the standard 4 suits, ordered
 * by the standard rules of Bridge (i.e., clubs (low), through spades (high).
 * 
 * @author UMD CS Dept.
 *
 */
public enum Suits 
	{clubs, diamonds, hearts, spades};
